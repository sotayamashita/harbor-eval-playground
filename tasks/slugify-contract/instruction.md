# Fix slugify

You are working in the task workspace.

Fix `slugify.py` so that `slugify(text)` follows the contract in `docs/slug-contract.md`.

You may run the visible tests:

```bash
python -m pytest tests/test_slugify.py
```

Do not edit the contract file.
Do not edit the visible tests.
